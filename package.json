{
  "name": "ainews-content",
  "version": "0.1.0",
  "description": "AINews Weekly content pipeline. GitHub Actions generate digest.json, published to GitHub Pages, consumed by the iOS app.",
  "type": "module",
  "private": true,
  "scripts": {
    "generate:daily": "tsx src/generate-daily.ts",
    "generate:learn": "tsx src/generate-learn.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
