{
  "schemaVersion": 1,
  "weekOf": "2026-06-08T00:00:00Z",
  "reportGeneratedAt": "2026-06-09T09:53:15Z",
  "learnGeneratedAt": "2026-06-08T12:00:00Z",
  "report": [
    {
      "id": "openai-ipo-confidential-filing-2026-06-08",
      "title": "OpenAI files confidentially for IPO, one week after Anthropic",
      "summary": "OpenAI submitted a draft S-1 to the SEC on June 8, kicking off a dual-IPO race with Anthropic that Wall Street is calling the largest AI listings of 2026. The company signaled timing is undecided and flagged it may stay private longer — relevant context given WSJ reporting that OpenAI projects burning $85B in 2028 even after doubling revenue.",
      "category": "openai",
      "sourceURL": "https://techcrunch.com/2026/06/08/following-anthropic-openai-files-confidentially-for-ipo/",
      "significance": 7
    },
    {
      "id": "openai-chatgpt-lockdown-mode-2026-06-06",
      "title": "OpenAI ships ChatGPT Lockdown Mode to block prompt-injection exfiltration",
      "summary": "Lockdown Mode rolled out June 6 across Free, Plus, Pro, and self-serve Business plans — it cuts outbound network requests to reduce prompt-injection data-exfiltration risk at the cost of disabling some web-connected features. Builders running ChatGPT in sensitive-data workflows should test whether their integrations break under the new mode.",
      "category": "openai",
      "sourceURL": "https://thehackernews.com/",
      "significance": 6
    },
    {
      "id": "openai-dreaming-v3-memory-2026-06-04",
      "title": "OpenAI's Dreaming V3 memory ships to Plus and Pro users",
      "summary": "Dreaming V3 is a background-synthesis memory architecture that automatically builds user profiles without explicit 'remember this' commands — roughly 5x more compute-efficient than the prior system, which is what enabled free-tier access. Began rolling out to US Plus and Pro on June 4; drop-in improvement for anyone building on the ChatGPT memory API.",
      "category": "openai",
      "sourceURL": "https://www.buildfastwithai.com/blogs/ai-news-today-june-5-2026",
      "significance": 7
    },
    {
      "id": "anthropic-glasswing-expansion-june-2026",
      "title": "Anthropic expands Project Glasswing to 200 orgs, grants ENISA first EU Mythos access",
      "summary": "Glasswing grew to 200 total partner organizations across 15+ countries on June 2, adding power, water, healthcare, and hardware sectors; separately, EU cybersecurity agency ENISA became the first non-US entity to receive Claude Mythos access. Partners have now flagged 23,000 potential vulnerabilities in 1,000 open-source projects — Anthropic is using this as its enterprise-depth narrative ahead of the IPO.",
      "category": "anthropic",
      "sourceURL": "https://www.anthropic.com/news",
      "significance": 6
    },
    {
      "id": "anthropic-claude-partner-network-services-track-2026-06-03",
      "title": "Anthropic launches Services Track and Partner Hub for Claude Partner Network",
      "summary": "Announced June 3, the Services Track formalizes a structured channel for implementation and integration partners building on Claude. Builders scoping enterprise deals now have a cleaner path to co-sell and co-build with Anthropic's partner ecosystem.",
      "category": "anthropic",
      "sourceURL": "https://www.anthropic.com/news",
      "significance": 5
    },
    {
      "id": "microsoft-mai-models-build-2026",
      "title": "Microsoft ships MAI-Code-1-Flash and MAI-Thinking-1 at Build 2026",
      "summary": "At Build, Microsoft launched MAI-Code-1-Flash — its first in-house text-to-code model — and MAI-Thinking-1, a reasoning model available in private preview via Microsoft Foundry. Both moves are Microsoft's direct attempt to reduce margin leakage to OpenAI and Anthropic as those two race toward IPO.",
      "category": "tooling",
      "sourceURL": "https://www.cnbc.com/2026/06/02/microsoft-unveils-new-ai-models-lessen-reliance-on-openai-lower-costs.html",
      "significance": 7
    },
    {
      "id": "gemini-35-pro-pending-grok5-rumor-june-2026",
      "title": "Gemini 3.5 Pro still unscheduled; Grok 5 Polymarket odds collapse to ~12%",
      "summary": "Gemini 3.5 Flash is in production at $1.50/$9.00 per million tokens and already beats Gemini 3.1 Pro on coding benchmarks at 4x the speed, but Pro has no public model ID or release date despite Pichai's I/O stage promise. Grok 5 Polymarket odds for a June ship dropped from 68% to roughly 12% — both are watchlist items, not integration targets.",
      "category": "google",
      "sourceURL": "https://wavespeed.ai/blog/posts/june-2026-ai-launch-wave/",
      "significance": 5
    }
  ],
  "learn": [
    {
      "id": "claude-code-dynamic-workflows",
      "name": "Claude Code Dynamic Workflows",
      "category": "anthropic",
      "oneLineDescription": "Spawn hundreds of parallel sub-agents from Claude Code for large codebase tasks.",
      "estimatedSetupMinutes": 5,
      "detail": {
        "whatItDoes": "Dynamic Workflows is a Claude Code feature (available in v2.1.154+) where Claude writes an orchestration script for your task and then fans it out across many background sub-agents running in parallel. Each sub-agent operates in an isolated context window and reports back to a lead agent that synthesizes findings. You trigger a workflow with /workflows and manage runs from the same interface.\n\nUse cases include codebase-wide security audits, large framework migrations, incident post-mortems where sub-agents fan out through deploy history, error logs, metrics, and support tickets simultaneously. The full execution trace is visible in Claude Console — every sub-agent's actions, ordering, and reasoning are inspectable.",
        "whoItsFor": "Platform engineers, senior developers, and teams running large codebase migrations or multi-file audits who have previously had to wire together custom orchestration code to parallelize Claude tasks. Requires a Max, Team Premium, or Enterprise plan with Opus 4.8 as the default model.",
        "pros": [
          "Parallelizes tasks that would previously require custom orchestration scripts",
          "Full audit trail in Claude Console — every sub-agent action is inspectable",
          "Works with self-hosted sandboxes (public beta) so code never leaves your perimeter",
          "Automatically handles context management and error recovery across sub-agents"
        ],
        "cons": [
          "Token costs scale linearly with sub-agent count — large workflows can be expensive fast",
          "Requires Opus 4.8 (v2.1.154+); older versions will not support /workflows",
          "Still early — complex dependency graphs between sub-agents are not yet natively modeled"
        ],
        "setupGuideMarkdown": "## Prerequisites\n- Claude Code v2.1.154 or later (`claude --version`)\n- Max, Team Premium, Enterprise pay-as-you-go, or Anthropic API plan\n- Opus 4.8 set as your active model\n\n## Step 1: Update Claude Code\n\n```bash\nnpm update -g @anthropic-ai/claude-code\nclaude --version  # should show 2.1.154 or later\n```\n\n## Step 2: Set Opus 4.8 as your model\n\nInside a Claude Code session:\n\n```\n/model opus-4-8\n```\n\nOr pin it in your project settings file `.claude/settings.json`:\n\n```json\n{\n  \"model\": \"claude-opus-4-8-20260601\",\n  \"effort\": \"high\"\n}\n```\n\nFor harder tasks, bump effort:\n\n```\n/effort xhigh\n```\n\n## Step 3: Trigger a workflow\n\nNavigate to your project root and start Claude Code:\n\n```bash\ncd your-project\nclaude\n```\n\nDescribe a large task naturally — Claude will decide if a workflow is appropriate:\n\n```\n> Audit every file in src/ for SQL injection vulnerabilities, then generate a patch for each one found.\n```\n\nOr explicitly manage workflows:\n\n```\n/workflows          # list active and past workflow runs\n/workflows status   # check sub-agent progress\n```\n\n## Gotcha: Token cost\n\nWorkflows spawn many sub-agents, each with its own context. A codebase-wide audit on a large repo can consume 10–50M tokens. Check your usage in Claude Console before kicking off wide searches. Use `/effort high` (not `xhigh`) for exploratory passes to limit per-sub-agent thinking budget.\n\n## Step 4 (optional): Enable self-hosted sandboxes\n\nFor teams that need code to stay in their perimeter, enroll in the public beta via Claude Console under **Settings > Managed Agents > Sandboxes**, then point execution at your own infrastructure (Cloudflare, Modal, Vercel, or a custom endpoint).",
        "sourceURL": "https://releasebot.io/updates/anthropic/claude-code"
      }
    },
    {
      "id": "openai-codex-cli-rust",
      "name": "OpenAI Codex CLI",
      "category": "openai",
      "oneLineDescription": "Open-source, Rust-built terminal coding agent from OpenAI backed by GPT-5.5.",
      "estimatedSetupMinutes": 5,
      "detail": {
        "whatItDoes": "Codex CLI is OpenAI's terminal coding agent. It runs locally, can read and modify files in your working directory, and executes shell commands. It is built in Rust for speed, is open source (github.com/openai/codex), and is backed by GPT-5.5. It supports multi-agent v2 (each spawned thread keeps its own model runtime choice), plugin sharing for Enterprise workspaces, remote-control pairing so a colleague can attach to your session, and sandboxed code execution.\n\nThe June 2026 releases added multi-agent v2 with cleaner follow-up defaults for spawned agents, machine-readable `codex plugin list --json` output, hosted web and image tools inside code-mode flows, and standalone parallel web searches.",
        "whoItsFor": "Developers who want a ChatGPT-account-backed coding agent in the terminal without leaving OpenAI's ecosystem. ChatGPT Plus, Pro, Business, Edu, and Enterprise plans include Codex access. Teams on Enterprise get workspace-level plugin sharing so teammates can install shared local plugins from the Codex plugin directory.",
        "pros": [
          "Open source (Apache 2.0) — inspect and extend the Rust source",
          "Backed by GPT-5.5, which scores 82.7% on Terminal-Bench 2.0",
          "Multi-agent v2 supports parallel sub-agent threads each with independent runtime choices",
          "Single install script works on macOS, Linux, and Windows"
        ],
        "cons": [
          "Tightly coupled to GPT-5.5 — no support for non-OpenAI models",
          "Requires a paid ChatGPT plan; free tier does not include Codex",
          "Windows sandbox support is still alpha (`codex sandbox setup --elevated`)"
        ],
        "setupGuideMarkdown": "## Prerequisites\n- macOS or Linux (Windows alpha available)\n- ChatGPT Plus, Pro, Business, Edu, or Enterprise plan\n- No other dependencies — the installer is self-contained\n\n## Step 1: Install\n\n**macOS / Linux**\n```bash\ncurl -fsSL https://chatgpt.com/codex/install.sh | sh\n```\n\n**Windows (PowerShell)**\n```powershell\npowershell -ExecutionPolicy ByPass -c \"irm https://chatgpt.com/codex/install.ps1 | iex\"\n```\n\nAlternatively, install via Homebrew:\n```bash\nbrew install openai-codex\n```\n\n## Step 2: Authenticate\n\n```bash\ncodex login\n# Opens browser — sign in with your ChatGPT account\n# Or use an API key:\nexport OPENAI_API_KEY=\"sk-...\"\n```\n\n## Step 3: Run in your project\n\n```bash\ncd your-project\ncodex\n```\n\nCode-mode with a single prompt (non-interactive):\n```bash\ncodex -p \"Add input validation to every POST endpoint in src/routes/\"\n```\n\n## Step 4: Enable plugins (Enterprise)\n\nList installed plugins:\n```bash\ncodex plugin list --json\n```\n\nInstall a plugin from the marketplace:\n```bash\ncodex plugin install <plugin-name>\n```\n\nShare a plugin with your workspace so teammates can install it:\n```bash\ncodex plugin share <plugin-name>\n```\n\n## Gotcha: Unattended / CI installs\n\nSet `CODEX_NON_INTERACTIVE=1` before running the installer in CI to skip all prompts:\n```bash\nexport CODEX_NON_INTERACTIVE=1\ncurl -fsSL https://chatgpt.com/codex/install.sh | sh\n```\n\nThen authenticate via API key rather than browser OAuth:\n```bash\nexport OPENAI_API_KEY=\"sk-...\"\ncodex -p \"Run the test suite and report failures\" --output-format json\n```",
        "sourceURL": "https://github.com/openai/codex"
      }
    },
    {
      "id": "gemini-cli-google-open-source",
      "name": "Gemini CLI (google-gemini/gemini-cli)",
      "category": "google",
      "oneLineDescription": "Free, open-source terminal AI agent with 1M-token context and 1,000 req/day free tier.",
      "estimatedSetupMinutes": 3,
      "detail": {
        "whatItDoes": "Gemini CLI is Google's open-source (Apache 2.0) terminal AI agent. It uses a ReAct (reason-and-act) loop with built-in tools — file operations, shell commands, web fetching, and Google Search grounding — plus MCP support for custom integrations. The free tier with a personal Google account gives 60 req/min and 1,000 req/day against Gemini models with a 1M-token context window.\n\nNote: Google announced at I/O 2026 that Gemini CLI will transition to Antigravity CLI for consumer users by June 18, 2026. Gemini CLI continues to work for paid Gemini Code Assist Standard/Enterprise license holders. If you are on a free Google account, plan to migrate to Antigravity CLI. The install and usage commands below work today for the Gemini CLI stable release.",
        "whoItsFor": "Developers who want a free, zero-lock-in terminal AI agent for everyday coding tasks, long-context codebase reads, or Google Cloud-native automation. Also a strong choice for teams already on Vertex AI — authentication integrates cleanly with existing IAM. On hard multi-file refactors, Claude Code still leads on SWE-bench Verified; for free exploration and long-context reads, Gemini CLI is the most cost-effective starting point.",
        "pros": [
          "Genuinely free: 1,000 req/day with a personal Google account, no credit card required",
          "1M-token context window beats most competitors' defaults by 2.5-7x",
          "Open source under Apache 2.0 — inspect, fork, and contribute",
          "Built-in Google Search grounding directly inside the agent loop"
        ],
        "cons": [
          "Transitioning to Antigravity CLI for consumer users on June 18 — new installs of Gemini CLI will not receive feature updates",
          "Scores ~12 points behind Claude Opus 4.7 on SWE-bench Verified for hard multi-file refactors",
          "The --yolo auto-approve flag will execute any shell command without confirmation — avoid outside a sandbox"
        ],
        "setupGuideMarkdown": "## Prerequisites\n- Node.js 18 or later (`node --version`)\n- A Google account (for the free tier)\n\n## Step 1: Install\n\n```bash\nnpm install -g @google/gemini-cli\n```\n\nOr run without installing:\n```bash\nnpx @google/gemini-cli\n```\n\n## Step 2: Authenticate\n\n**Personal Google account (free tier — 1,000 req/day):**\n```bash\ngemini login\n# Opens browser — sign in with your Google account\n```\n\n**API key (pay-as-you-go, no daily cap):**\n```bash\n# Get a key at https://aistudio.google.com/apikey\nexport GEMINI_API_KEY=\"AIza...\"\n```\n\n**Vertex AI (enterprise/GCP):**\n```bash\nexport GOOGLE_API_KEY=\"YOUR_API_KEY\"\nexport GOOGLE_GENAI_USE_VERTEXAI=true\ngemini\n```\n\n## Step 3: Run in your project\n\n```bash\ncd your-project\ngemini\n```\n\nSingle-shot prompt:\n```bash\ngemini -p \"Explain the architecture of this codebase\"\n```\n\nAttach a specific file:\n```bash\ngemini -f src/auth.ts -p \"Review this for security issues\"\n```\n\nHeadless/CI mode:\n```bash\ngemini --headless -p \"Run tests and report failures\" --output-format json\n```\n\n## Step 4: Per-project config\n\nCreate a `GEMINI.md` at your project root to give persistent context:\n```markdown\n# Project Context\nThis is a Django 5.x API. Always use type hints. Tests use pytest.\n```\n\nGemini CLI reads this file automatically at session start.\n\n## Gotchas\n\n**`command not found: gemini`** — npm global bin is not on your PATH.\n```bash\nnpm config get prefix\n# Add $prefix/bin to your shell profile, e.g.:\necho 'export PATH=\"$(npm config get prefix)/bin:$PATH\"' >> ~/.zshrc\nsource ~/.zshrc\n```\n\n**`EACCES` on `npm install -g`** — Switch to nvm or run:\n```bash\nnpm config set prefix ~/.npm-global\nexport PATH=\"$HOME/.npm-global/bin:$PATH\"\nnpm install -g @google/gemini-cli\n```\n\n**Migration to Antigravity CLI (June 18+):** Enterprise/paid users stay on Gemini CLI. Free users should follow Google's migration guide at developers.googleblog.com.",
        "sourceURL": "https://github.com/google-gemini/gemini-cli"
      }
    },
    {
      "id": "microsoft-mai-thinking-1-foundry",
      "name": "MAI-Thinking-1 on Microsoft Foundry",
      "category": "other",
      "oneLineDescription": "Microsoft's first in-house reasoning model: 35B active params, 256K context, private preview on Foundry.",
      "estimatedSetupMinutes": 15,
      "detail": {
        "whatItDoes": "MAI-Thinking-1 is the flagship model from Microsoft's seven-model MAI family, shipped at Build 2026. It is a 35B active-parameter reasoning model with a 256K context window, trained from scratch without distillation from any third-party model on enterprise-grade, commercially licensed data. Microsoft claims it matches Claude Opus 4.6 on SWE-bench Pro coding benchmarks and was preferred over Claude Sonnet 4.6 in blind evaluations — though these results have not been independently verified.\n\nThe model is currently in private preview on Azure Foundry (formerly Azure AI Studio). Access requires a Foundry workspace and an approved private-preview enrollment. It is model-agnostic in the sense that the same Foundry endpoint and billing relationship works for all 11,000+ models in the catalog, including OpenAI GPT-5.5 and Anthropic Claude Opus 4.8/Sonnet 4.5.",
        "whoItsFor": "Enterprise teams already in the Azure ecosystem who need a reasoning model with clean data provenance for compliance-sensitive workloads. The low-token-cost positioning also makes it a candidate for high-volume agentic pipelines where GPT-5.5 costs are prohibitive. Not yet suitable for teams that need a stable public GA model — private preview status means availability and API shape can change.",
        "pros": [
          "Trained on commercially licensed data with auditable provenance — relevant for regulated industries",
          "256K context window at reportedly low token cost compared to GPT-5.5",
          "Same Foundry endpoint and billing for all 11,000+ catalog models — no additional integration work",
          "Model Router can auto-switch between MAI-Thinking-1 and other models at runtime for cost/performance"
        ],
        "cons": [
          "Private preview only — no guaranteed SLA, API shape may change before GA",
          "Microsoft's benchmark claims (vs Claude Sonnet/Opus 4.6) are self-reported and not independently verified",
          "Requires Azure Foundry workspace setup and private-preview enrollment — not accessible in minutes"
        ],
        "setupGuideMarkdown": "## Prerequisites\n- Azure subscription with an active resource group\n- Azure CLI installed (`az --version`) and authenticated (`az login`)\n- Private-preview enrollment for MAI-Thinking-1 (request at https://aka.ms/MAI-Thinking-1-preview)\n\n## Step 1: Create a Foundry project\n\n```bash\n# Install the Azure AI Foundry SDK\npip install azure-ai-projects azure-identity\n\n# Create a Foundry hub and project via CLI\naz ml workspace create \\\n  --name my-foundry-project \\\n  --resource-group my-rg \\\n  --kind hub\n```\n\nOr navigate to https://ai.azure.com and create a new project under your subscription.\n\n## Step 2: Deploy MAI-Thinking-1\n\nIn the Foundry portal:\n1. Go to **Model catalog** → search `MAI-Thinking-1`\n2. Click **Deploy** → choose **Serverless (pay-as-you-go)**\n3. Name your deployment (e.g., `mai-thinking-1-prod`)\n4. Copy the **endpoint URL** and **API key** from the deployment page\n\n## Step 3: Call the endpoint\n\n```python\nimport os\nfrom openai import AzureOpenAI\n\nclient = AzureOpenAI(\n    azure_endpoint=os.environ[\"AZURE_FOUNDRY_ENDPOINT\"],\n    api_key=os.environ[\"AZURE_FOUNDRY_API_KEY\"],\n    api_version=\"2025-01-01-preview\",\n)\n\nresponse = client.chat.completions.create(\n    model=\"mai-thinking-1-prod\",  # your deployment name\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a senior software engineer.\"},\n        {\"role\": \"user\", \"content\": \"Review this Python function for edge cases: ...\"}\n    ],\n    max_tokens=4096,\n)\n\nprint(response.choices[0].message.content)\n```\n\n## Step 4: Use Model Router for automatic fallback\n\n```python\n# Point at the Foundry Model Router endpoint instead of a specific deployment\nclient = AzureOpenAI(\n    azure_endpoint=os.environ[\"AZURE_FOUNDRY_ROUTER_ENDPOINT\"],\n    api_key=os.environ[\"AZURE_FOUNDRY_API_KEY\"],\n    api_version=\"2025-01-01-preview\",\n)\n# Model Router picks MAI-Thinking-1 for reasoning tasks, Flash models for simple queries\n```\n\n## Gotcha: Private preview gating\n\nMAI-Thinking-1 will return a `404` or `ModelNotFound` error if your subscription has not been approved for private preview. The enrollment link is https://aka.ms/MAI-Thinking-1-preview. Approvals have been taking 3-5 business days as of Build week.",
        "sourceURL": "https://blogs.microsoft.com/blog/2026/06/02/microsoft-build-2026-be-yourself-at-work/"
      }
    }
  ]
}
